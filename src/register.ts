const inputs: NodeListOf<HTMLInputElement> = document.querySelectorAll("input");
const form: HTMLElement | null = document.getElementById("register-form");
const first_name: HTMLElement | null = document.getElementById("first_name");
const last_name: HTMLElement | null = document.getElementById("last_name");
const email: HTMLElement | null = document.getElementById("email");
const date_of_birth: HTMLElement | null =
  document.getElementById("date_of_birth");
const password: HTMLElement | null = document.getElementById("password");
const confirm_password: HTMLElement | null =
  document.getElementById("confirm_password");
